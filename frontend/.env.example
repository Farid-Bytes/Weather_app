# Local: leave empty. Vite proxies /weather, /search, and /chat to localhost:8000.
# Production (Vercel): set this in the Vercel dashboard, not in a committed file.
# Example: https://your-api.onrender.com
VITE_API_BASE=
